// ─── 1. Defaults ────────────────────────────────────────────────
export const MAX_RENDERED_NODES_DEFAULT = 800;
export const MAX_RENDERED_EDGES_DEFAULT = 8;
export const MAX_DEPTH_DEFAULT = 3;
export const RENDER_SPEED_DEFAULT = 12;

// ─── 2. Bounds / configuration ──────────────────────────────────
export const MIN_POSSIBLE_NODES = 50;
export const MAX_POSSIBLE_NODES = 2000;
export const NODES_STEP = 50;

export const MIN_POSSIBLE_EDGES = 0;
export const MAX_POSSIBLE_EDGES = 50;
export const EDGES_STEP = 1;

export const MIN_POSSIBLE_DEPTH = 1;
export const MAX_POSSIBLE_DEPTH = 5;

export const MIN_RENDER_SPEED = 5;
export const MAX_RENDER_SPEED = 200;
export const RENDER_SPEED_STEP = 5;
